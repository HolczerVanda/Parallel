#ifndef MATRIX_H
#define MATRIX_H

/**
 * Prints the matrix.
 */
void printMatrix(int matrix[3][3]);

/**
 * Decides whether a matrix is a unit matrix or not.
 */
void isUnitMatrix(int matrix[3][3]);

/**
 * Decides whether a matrix is diagonal or not.
 */
void isDiagonalMatrix(int matrix[3][3]);

/**
 * Decides whether a matrix is symmetric or not.
 */
void isSymmetricMatrix(int matrix[3][3]);

#endif /* MATRIX_H */